{
  "name": "synthreat",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate:content && npm run generate:ai-index && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "validate:content": "node scripts/validate-content.mjs",
    "generate:ai-index": "node scripts/generate-ai-index.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "mermaid": "^12.0.0"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "jsdom": "^29.1.1"
  }
}
